# Copy to .env.local and fill in from the Supabase dashboard (EU project).
# Never commit .env.local. Only the public anon key belongs here — never the service_role key.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
