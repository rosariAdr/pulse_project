# /design — approved mockups

Claude Code implements **from these approved mockups**, not from prose (Build Plan, Step 1).

Add one folder per validated journey, in this order:

1. `01-student-test-day/` — sign in → first-visit account claim → entrance test → my results (mobile-first)
2. `02-teacher-prepares/` — module space: entrance-test builder, sessions setup, prepare a session, raw results
3. `03-master-prepares/` — create modules, classes + student profiles, attach classes

When a journey is added here, reference it from `CLAUDE.md`.
